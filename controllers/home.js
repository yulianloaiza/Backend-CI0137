/*const listSuccessStories = require("../dataSuccessStories.json");

exports.listSuccessStories = async (req, res) => {
    // #swagger.tags = ['SuccessStories']}
    try {
      const result = listSuccessStories;
      res.json(listSuccessStories);
    } catch (error) {
      res.status(500).send("GET error on Success Stories: " + error);
    }
  }
*/